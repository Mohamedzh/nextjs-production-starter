# ============================================
# NEXT.JS RAILWAY PRODUCTION STARTER
# Environment Variables
# ============================================
# Required Node.js version: 20.19.0

# Node Environment
NODE_ENV=development

# ============================================
# REQUIRED
# ============================================

# --------------------------------------------
# PostgreSQL Database (Prisma)
# Required: DATABASE_URL
# Enables: Prisma ORM, database sessions, user persistence
# --------------------------------------------

# Local development with Docker Compose:
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/nextjs_starter

# Railway auto-provides this when PostgreSQL service is added:
# DATABASE_URL=${Postgres.DATABASE_URL}

# ============================================
# OPTIONAL FEATURES
# Each feature is automatically enabled when its required environment variables are set
# ============================================

# --------------------------------------------
# Authentication (NextAuth.js)
# Required: NEXTAUTH_SECRET
# Optional: NEXTAUTH_URL (auto-detected on Railway)
# Enables: /api/auth/* routes, session management
# --------------------------------------------

# Generate with: npx auth secret
# Or: openssl rand -base64 64
# NEXTAUTH_SECRET=your-super-secret-key-min-32-characters-long

# Auto-detected on Railway, set manually for local dev
# NEXTAUTH_URL=http://localhost:3000

# --------------------------------------------
# Redis Cache (OPTIONAL - Recommended for Production)
# Required: REDIS_URL
# Enables: Persistent ISR caching, faster page loads after deployment
# Fallback: Filesystem cache if not configured
# --------------------------------------------

# Local development with Docker Compose:
# REDIS_URL=redis://localhost:6379

# Railway auto-provides this when Redis service is added:
# REDIS_URL=${Redis.REDIS_URL}

# --------------------------------------------
# Cron Jobs (Scheduled Tasks)
# Required: CRON_SECRET
# Enables: Secure /api/cron endpoint
# --------------------------------------------

# Generate with: openssl rand -base64 32
# CRON_SECRET=your-cron-secret-min-32-characters

# --------------------------------------------
# OAuth Providers (Optional)
# Enable social login by setting provider credentials
# --------------------------------------------

# GitHub OAuth
# Get from: https://github.com/settings/developers
# Callback URL: {NEXTAUTH_URL}/api/auth/callback/github
# GITHUB_ID=your_github_oauth_app_id
# GITHUB_SECRET=your_github_oauth_app_secret

# Google OAuth
# Get from: https://console.cloud.google.com/apis/credentials
# Callback URL: {NEXTAUTH_URL}/api/auth/callback/google
# GOOGLE_ID=your_google_oauth_client_id
# GOOGLE_SECRET=your_google_oauth_client_secret

# Discord OAuth
# Get from: https://discord.com/developers/applications
# Callback URL: {NEXTAUTH_URL}/api/auth/callback/discord
# DISCORD_ID=your_discord_oauth_client_id
# DISCORD_SECRET=your_discord_oauth_client_secret

# --------------------------------------------
# Feature Matrix
# --------------------------------------------
# | Feature         | Required Env Vars        | Status    |
# |-----------------|--------------------------|-----------|
# | PostgreSQL      | DATABASE_URL             | REQUIRED  |
# | Authentication  | NEXTAUTH_SECRET          | Optional  |
# | Redis Cache     | REDIS_URL                | Optional  |
# | GitHub Login    | GITHUB_ID + SECRET       | Optional  |
# | Google Login    | GOOGLE_ID + SECRET       | Optional  |
# | Discord Login   | DISCORD_ID + SECRET      | Optional  |
# | Cron Jobs       | CRON_SECRET              | Optional  |
# --------------------------------------------
